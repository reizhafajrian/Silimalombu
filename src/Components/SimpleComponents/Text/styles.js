import styled from 'styled-components';

const TextBoldTitle3Component = styled.Text`
  font-weight: bold;
  font-size: 32px;
  color: ${props => props.color};
`;

const TextBoldTitle2Component = styled.Text`
  font-weight: bold;
  font-size: 24px;
  color: ${props => props.color};
`;

const TextBoldTitleComponent = styled.Text`
  font-weight: bold;
  font-size: 18px;
  color: ${props => props.color};
`;

const TextBoldSubTitleComponent = styled.Text`
  font-weight: bold;
  font-size: 14px;
  color: ${props => props.color};
`;

const TextBoldDescriptionComponent = styled.Text`
  font-weight: bold;
  font-size: 12px;
  color: ${props => props.color};
`;

const TextRegularTitle3Component = styled.Text`
  font-weight: normal;
  font-size: 32px;
  color: ${props => props.color};
`;

const TextRegularTitle2Component = styled.Text`
  font-weight: normal;
  font-size: 24px;
  color: ${props => props.color};
`;

const TextRegularTitleComponent = styled.Text`
  font-weight: normal;
  font-size: 18px;
  color: ${props => props.color};
`;

const TextRegularSubTitleComponent = styled.Text`
  font-weight: normal;
  font-size: 14px;
  color: ${props => props.color};
`;

const TextRegularDescriptionComponent = styled.Text`
  font-weight: normal;
  font-size: 12px;
  color: ${props => props.color};
`;

const TextLightTitle3Component = styled.Text`
  font-weight: normal;
  font-size: 32px;
  color: ${props => props.color};
`;

const TextLightTitle2Component = styled.Text`
  font-weight: normal;
  font-size: 24px;
  color: ${props => props.color};
`;

const TextLightTitleComponent = styled.Text`
  font-weight: normal;
  font-size: 18px;
  color: ${props => props.color};
`;

const TextLightSubTitleComponent = styled.Text`
  font-weight: normal;
  font-size: 14px;
  color: ${props => props.color};
`;

const TextLightDescriptionComponent = styled.Text`
  font-weight: normal;
  font-size: 12px;
  color: ${props => props.color};
`;

export {
  TextBoldTitle3Component,
  TextBoldTitle2Component,
  TextBoldTitleComponent,
  TextBoldSubTitleComponent,
  TextBoldDescriptionComponent,
  TextRegularTitle3Component,
  TextRegularTitle2Component,
  TextRegularTitleComponent,
  TextRegularSubTitleComponent,
  TextRegularDescriptionComponent,
  TextLightTitle3Component,
  TextLightTitle2Component,
  TextLightTitleComponent,
  TextLightSubTitleComponent,
  TextLightDescriptionComponent,
};
